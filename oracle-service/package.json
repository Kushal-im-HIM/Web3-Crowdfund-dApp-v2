{
  "name": "milestone-oracle",
  "version": "1.0.0",
  "description": "Off-chain oracle that listens for MilestoneSubmitted events and verifies evidence",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest --runInBand --forceExit",
    "test:coverage": "jest --coverage --runInBand --forceExit"
  },
  "dependencies": {
    "ethers": "^5.7.2",
    "axios": "^1.6.2",
    "dotenv": "^16.3.1",
    "kubo-rpc-client": "^3.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2"
  }
}
